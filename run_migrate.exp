#!/usr/bin/expect -f
set timeout -1
spawn npx prisma migrate dev --name cashier_role
expect {
    "Are you sure you want to create and apply this migration" {
        send "y\r"
        exp_continue
    }
    eof
}
